# Copy to .env.local and fill in your Supabase values.
# Without these, the app uses AsyncStorage (device-local) only.
EXPO_PUBLIC_SUPABASE_URL=your_supabase_url
EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your_supabase_anon_key
